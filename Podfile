# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'WeboniseAssignment' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'GooglePlaces'
  #pod 'GooglePlacePicker'
  #pod 'GoogleMaps'
  # Pods for WeboniseAssignment

  target 'WeboniseAssignmentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WeboniseAssignmentUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
